const d=loadContent();if(editTitle){editTitle.value=d.site.title;editSubtitle.value=d.site.subtitle;editLogo.value=d.site.logo||'';candidateEditor.innerHTML=d.candidates.map((c,i)=>`<div class="editor-card"><b>เบอร์ ${c.no}</b><input data-i="${i}" data-k="name" value="${c.name}"><input data-i="${i}" data-k="team" value="${c.team}"><input data-i="${i}" data-k="image" value="${c.image||''}" placeholder="ลิงก์รูปภาพ"></div>`).join('');saveContent.onclick=()=>{let x=JSON.parse(JSON.stringify(d));x.site={title:editTitle.value,subtitle:editSubtitle.value,logo:editLogo.value};candidateEditor.querySelectorAll('input[data-i]').forEach(i=>x.candidates[i.dataset.i][i.dataset.k]=i.value);saveContentText(x);alert('บันทึกข้อมูลแล้ว')}}function saveContentText(x){window.saveContent(x)}start?.addEventListener('click',()=>window.demo.start());stop?.addEventListener('click',()=>window.demo.stop());
