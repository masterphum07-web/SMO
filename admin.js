document.addEventListener('DOMContentLoaded',()=>{const gate=document.getElementById('loginGate'),app=document.getElementById('adminApp'),form=document.getElementById('loginForm'),unlock=()=>{gate.classList.add('hidden');app.classList.remove('hidden');sessionStorage.setItem('election-admin-auth','1')};if(sessionStorage.getItem('election-admin-auth')==='1')unlock();form.addEventListener('submit',e=>{e.preventDefault();if(loginUser.value==='admin'&&loginPass.value==='0648587699')unlock();else loginError.textContent='ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง'});logout.addEventListener('click',()=>{sessionStorage.removeItem('election-admin-auth');location.reload()});let draft=JSON.parse(JSON.stringify(loadContent()));const editor=document.getElementById('candidateEditor');const fileToData=(input,cb)=>{const f=input.files[0];if(!f)return;const r=new FileReader();r.onload=()=>cb(r.result);r.readAsDataURL(f)};const draw=()=>{editor.innerHTML=draft.candidates.map((c,i)=>`<div class="editor-card"><div class="editor-line"><b>เบอร์ ${c.no}</b><button type="button" class="remove-candidate" data-index="${i}">ลบ</button></div><input data-i="${i}" data-k="no" value="${c.no}" placeholder="หมายเลข"><input data-i="${i}" data-k="name" value="${c.name}" placeholder="ชื่อผู้สมัคร"><input data-i="${i}" data-k="team" value="${c.team}" placeholder="ทีม / พรรค"><label>รูปผู้สมัคร<input type="file" accept="image/*" data-file="image" data-i="${i}"></label><input data-i="${i}" data-k="image" value="${c.image||''}" placeholder="หรือวางลิงก์รูปภาพ"><label>รูปนโยบาย / รูปเพิ่มเติม<input type="file" accept="image/*" multiple data-file="policyImages" data-i="${i}"></label><textarea data-i="${i}" data-k="policy" placeholder="ข้อความนโยบาย">${c.policy||''}</textarea></div>`).join('');editor.querySelectorAll('input[data-k],textarea[data-k]').forEach(x=>x.addEventListener('input',()=>draft.candidates[x.dataset.i][x.dataset.k]=x.value));editor.querySelectorAll('input[data-file]').forEach(x=>x.addEventListener('change',()=>{if(x.dataset.file==='image')fileToData(x,v=>draft.candidates[x.dataset.i].image=v);else{draft.candidates[x.dataset.i].policyImages=[];[...x.files].forEach(f=>{const r=new FileReader();r.onload=()=>{draft.candidates[x.dataset.i].policyImages.push(r.result)};r.readAsDataURL(f)})}}));editor.querySelectorAll('.remove-candidate').forEach(x=>x.addEventListener('click',()=>{if(draft.candidates.length>1){draft.candidates.splice(+x.dataset.index,1);draw()}}))};editTitle.value=draft.site.title;editSubtitle.value=draft.site.subtitle;editLogo.value=draft.site.logo||'';draw();document.getElementById('logoFile')?.addEventListener('change',e=>fileToData(e.target,v=>{draft.site.logo=v}));start.addEventListener('click',()=>demo.start());stop.addEventListener('click',()=>demo.stop());addCandidate.addEventListener('click',()=>{const used=draft.candidates.map(c=>+c.no);let no=1;while(used.includes(no))no++;draft.candidates.push({no:String(no),name:'ผู้สมัครใหม่',team:'ทีมใหม่',image:'',policy:''});draw()});saveContent.addEventListener('click',()=>{draft.site={title:editTitle.value.trim()||'การเลือกตั้ง',subtitle:editSubtitle.value.trim(),logo:draft.site.logo||editLogo.value.trim()};saveContentData(draft);alert('บันทึกข้อมูลเรียบร้อยแล้ว')});function saveContentData(x){window.saveContent(x)}});
